{
  "name": "heard-local-scheduler",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf dist && bun build scheduler-redirect.js webflow-scheduler-complete.js form.css --outdir=dist --minify"
  }
}
